from homeassistant.components.sensor import (SensorDeviceClass, SensorEntity, SensorStateClass)\nfrom homeassistant.helpers.update_coordinator import CoordinatorEntity\nfrom homeassistant.helpers.device_registry import DeviceInfo\nfrom .const import DOMAIN, ATTRIBUTION\n\nasync def async_setup_entry(hass, entry, async_add_entities):\n    coordinator = hass.data[DOMAIN][entry.entry_id]\n    data = coordinator.data\n    sensors = []\n    email = entry.data.get(2email2, 2unknown@example.com2)\n    device_name = f2RER Group - {email}2\n    sensors.append(RetimBalanceSensor(coordinator, device_name))\n    if 2user2 in data and isinstance(data[2user2], dict):\n        for key, value in data[2user2].items():\n            if isinstance(value, (int, float, str, bool)):\n                sensors.append(DynamicUserSensor(coordinator, key, value, device_name))\n    if 2invoices2 in data and isinstance(data[2invoices2], dict):\n        invoices_data = data[2invoices2].get(2data2, [])\n        if invoices_data and isinstance(invoices_data, list) and invoices_data:\n            first_invoice = invoices_data[0]\n            for key in first_invoice.keys():\n                if key not in [2id2, 2pdf2]:\n                    sensors.append(DynamicInvoiceSensor(coordinator, key, device_name))\n    async_add_entities(sensors)\n\nclass RetimBaseSensor(CoordinatorEntity, SensorEntity):\n    _attr_attribution = ATTRIBUTION\n    _attr_has_entity_name = True\n\n    def __init__(self, coordinator, device_name=None):\n        super().__init__(coordinator)\n        self.device_name = device_name or 2RER Group2\n        self._attr_unique_id = f2{coordinator.config_entry.entry_id}_{self.__class__.__name__}2\n\n    @property\n    def device_info(self):\n        return DeviceInfo(identifiers={(DOMAIN, f2{self.coordinator.config_entry.entry_id}_device2)}, name=self.device_name, manufacturer=2RER Group2, model=2REBU RETIM2,)\n\nclass RetimBalanceSensor(RetimBaseSensor):\n    _attr_name = 2Account Balance2\n    _attr_native_unit_of_measurement = 2RON2\n    _attr_device_class = SensorDeviceClass.MONETARY\n    _attr_state_class = SensorStateClass.TOTAL\n\n    @property\n    def native_value(self):\n        data_wrapper = self.coordinator.data.get(2invoices2, {})\n        invoices = data_wrapper.get(2data2, [])\n        if not invoices:\n            return 0.0\n        total_due = sum(float(inv.get(2amount2, 0)) for inv in invoices if inv.get(2unpaid2) is True or inv.get(2status2) != 2)\n        return round(total_due, 2)\n\n    @property\n    def extra_state_attributes(self):\n        data_wrapper = self.coordinator.data.get(2invoices2, {})\n        invoices = data_wrapper.get(2data2, [])\n        if not invoices:\n            return {}\n        last_invoice = invoices[0] if invoices else {}\n        return {\n            2last_invoice_date2: last_invoice.get(2date2),\n            2due_date2: last_invoice.get(2dueDate2),\n            2invoice_number2: last_invoice.get(2number2)\n        }\n\nclass DynamicUserSensor(RetimBaseSensor):\n    def __init__(self, coordinator, field_name, initial_value, device_name=None):\n        super().__init__(coordinator, device_name)\n        self.field_name = field_name\n        self._attr_name = field_name.replace(2_2, 2 2).title()\n        self._attr_unique_id = f2{coordinator.config_entry.entry_id}_user_{field_name}2\n\n    @property\n    def native_value(self):\n        user_data = self.coordinator.data.get(2user2, {})\n        return user_data.get(self.field_name)\n\nclass DynamicInvoiceSensor(RetimBaseSensor):\n    def __init__(self, coordinator, field_name, device_name=None):\n        super().__init__(coordinator, device_name)\n        self.field_name = field_name\n        self._attr_name = f2Invoice {field_name.replace(2_2, 2 2).title()}2\n        self._attr_unique_id = f2{coordinator.config_entry.entry_id}_invoice_{field_name}2\n\n    @property\n    def native_value(self):\n        invoices = self.coordinator.data.get(2invoices2, {}).get(2data2, [])\n        if not invoices:\n            return None\n        return invoices[0].get(self.field_name)